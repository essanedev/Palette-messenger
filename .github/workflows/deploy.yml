name: Dokploy Deployment Workflow

on:
  workflow_run:
    workflows: [Django CI, Pylint]
    types: [completed]
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy App to Dokploy
        uses: qbix-tech/dokploy-deploy-action@v1
        with:
          dokploy_url: https://dokploy.messengerpalette.ru
          api_key: ${{ secrets.DOKPLOY_AUTH_TOKEN }}
          project_name: palette
          app_name: Backend
